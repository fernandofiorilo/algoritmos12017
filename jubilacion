#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;
void main()
{
	int edad,sexo,n,hijos ;
	   cout<<"Ingrese la edad" ;
	   cin>> edad;
	   cout<< "sexo: 1 masculino y 2= femenino" ;
	   cin>> sexo;
	if(sexo==1)
	  if(edad >=65)
		 cout<<"puede jubilarse" ;
	  else
		 cout<<"No tiene la edad suficiente" ;
	else
	   cout<<"cuantos hijos tiene" ;
	    cin>>hijos;
	   if(edad>=(60-hijos))
		 cout<<"se puede jubilar" ;
	   else
		 cout<<"No tiene la edad suficiente para jubilarse" ;
       
	getch();
 }
